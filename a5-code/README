Calculon is a simple interpeter that takes a small list of language constructs and evaluates them. 
Calculon is comprised of a lexer (calclex.ml), a parser (calcparse.ml), an evaluator (calceval.ml), and a top level interactive layer (calculon.ml)
